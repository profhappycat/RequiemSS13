/datum/preferences/proc/set_physique(amount)
	stats.set_stat(amount, STAT_PHYSIQUE)

/datum/preferences/proc/set_stamina(amount)
	stats.set_stat(amount, STAT_STAMINA)

/datum/preferences/proc/set_charisma(amount)
	stats.set_stat(amount, STAT_CHARISMA)

/datum/preferences/proc/set_composure(amount)
	stats.set_stat(amount, STAT_COMPOSURE)

/datum/preferences/proc/set_wits(amount)
	stats.set_stat(amount, STAT_WITS)

/datum/preferences/proc/set_resolve(amount)
	stats.set_stat(amount, STAT_RESOLVE)

