/mob/living/proc/set_physique(amount)
	stats.set_stat(amount, STAT_PHYSIQUE)

/mob/living/proc/set_stamina(amount)
	stats.set_stat(amount, STAT_STAMINA)

/mob/living/proc/set_charisma(amount)
	stats.set_stat(amount, STAT_CHARISMA)

/mob/living/proc/set_composure(amount)
	stats.set_stat(amount, STAT_COMPOSURE)

/mob/living/proc/set_wits(amount)
	stats.set_stat(amount, STAT_WITS)

/mob/living/proc/set_resolve(amount)
	stats.set_stat(amount, STAT_RESOLVE)

